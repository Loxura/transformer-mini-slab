# Optional. Copy to .env (gitignored) only if you need to change the published host port.
# Default is 11434 (the canonical Ollama port) — leave this unset for the normal drop-in setup.
# Set it to e.g. 11435 to run alongside a still-active host `ollama` service.
OLLAMA_HOST_PORT=11434
