# Autologin minideck on tty1 so the deck starts at boot.
# Install to: /etc/systemd/system/getty@tty1.service.d/autologin.conf  (then daemon-reload)
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin minideck --noclear %I $TERM
