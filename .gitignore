# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~

# local state / secrets
*.log
*.bak
.env

# *arr / slskd / qBittorrent runtime state (contains API keys & credentials)
server/config/
server/downloads/
server/library/

# never commit the music library itself
/music/
*.mp3
*.flac
*.m4a
*.ogg

# built eww binary (cross-built, not committed)
eww/eww
*.rlib
